# Random initial grid generated from the config file itself (no need to
# pass --random on the command line): the random= key takes the same
# WxH:PERCENT format as the CLI flag and replaces the PATTERN section.
rule=B3/S23
cell_size=8
speed_ms=10
edge=torus
random=80x120:55
