# Malformed rule string: no '/' separator, fails parse_rule()'s format
# check. Exercises the error path in srcs/parse_rule.c (the ft_split
# result is never freed before error_exit()) and, one level up, the
# `lines` split in srcs/parse_config.c.
rule=INVALID
PATTERN
.o.
..o
ooo
