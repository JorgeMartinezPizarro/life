# Ragged pattern: row 2 is a different length than row 1, which
# srcs/parse_pattern.c's pattern_width() rejects as non-rectangular.
# `lines` (the outer split from parse_config.c) is still live at that
# point and unfreed before error_exit().
rule=B3/S23
PATTERN
.o.
..o.
ooo
