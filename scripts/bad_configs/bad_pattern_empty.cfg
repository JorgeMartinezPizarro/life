# Empty pattern: the PATTERN marker has no rows after it, so
# pattern_height() returns 0 and srcs/parse_pattern.c calls
# error_exit() before any grid is allocated -- again with `lines`
# from parse_config.c still unfreed.
rule=B3/S23
PATTERN
