# Config declares both random= and a PATTERN section, which
# srcs/parse_config.c explicitly rejects. By the time it does, the
# random= grid has already been allocated and the `lines` split is
# still live, so this exercises another unfreed-on-error_exit path.
random=20x20:30
PATTERN
.o.
..o
ooo
